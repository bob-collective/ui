import { StyledObject } from 'styled-components';

type ScrollTheme = {
  base: StyledObject<object>;
  thumb: StyledObject<object>;
  button: StyledObject<object>;
  corner: StyledObject<object>;
};

export type { ScrollTheme };

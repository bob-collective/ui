'use client';

import { Flex } from '@gobob/ui';

// MEMO: just some placeholder for a future footer (for now just being used for spacing)
const Footer = (): JSX.Element => {
  return <Flex paddingY='4xl' />;
};

export { Footer };

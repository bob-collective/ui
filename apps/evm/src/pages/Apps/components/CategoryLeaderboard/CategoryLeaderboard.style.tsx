import { Card, H3 } from '@gobob/ui';
import styled from 'styled-components';

const StyledList = styled(Card)`
  border-top-left-radius: 0px;
`;

const StyledHeader = styled(H3)`
  position: absolute;
`;

export { StyledList, StyledHeader };
